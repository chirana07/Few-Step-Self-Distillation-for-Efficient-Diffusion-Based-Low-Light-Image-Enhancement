# Result inclusion ledger

This ledger prevents result removal or promotion based on assumptions. Each
paper item must identify its checkpoint, protocol, and supporting files.

## Retained

### Original transferred-teacher protocol

- FSD: 22.9094 PSNR / 0.8146 SSIM / 0.1914 LPIPS.
- Anchor-only: 21.5000 / 0.8044 / 0.2192.
- Evidence: exact saved outputs, recomputed PSNR/SSIM, post-hoc LPIPS on those
  same outputs, and paired bootstrap comparison.
- Protocol qualification: 639/50 training-derived split; checkpoints were
  selected using 20-step validation and evaluated at five steps.
- Uses: abstract, main table, literature table, teaser, and original-protocol
  step ablation.

### Frozen integrity protocol

- Transferred teacher: 22.5911 / 0.8133 / 0.1936.
- Anchor-only: 21.8720 / 0.8098 / 0.2097.
- FSD: 22.5620 / 0.8142 / 0.1939.
- ARR alpha selected on validation: 0.0.
- Evidence: immutable 620/69 manifest, common per-image test latents, official
  registry, per-image CSVs, protocol JSON files, and contribution summary.
- Uses: abstract robustness sentence, main table, methodology qualification,
  conclusion, and limitations.

### Fixed-checkpoint cross-dataset evaluation

- Exact original-protocol FSD checkpoint SHA-256:
  `5d7bac873b0b915fe6c0679b103fea9afe25f70de3958cab8da3d8779d156a37`.
- Five-step DDIM, ARR alpha 0, LPIPS-Alex, and fixed per-image latent seed
  99173 on every split.
- LOL-v1 eval15: 19.00 PSNR / 0.775 SSIM / 0.229 LPIPS, N=15.
- LOL-v2 Synthetic: 16.17 / 0.772 / 0.244, N=100.
- LSRW Huawei: 17.81 / 0.549 / 0.410, N=23 exact-size pairs.
- LSRW Nikon: 15.62 / 0.439 / 0.229, N=16 exact-size pairs.
- VE-LOL Synthetic: 14.71 / 0.482 / 0.559, N=100.
- Evidence: per-image CSVs, protocol JSON files, pairing audit, input
  baselines, and aggregate summary in `05_external_evaluation`.
- Use: fixed-checkpoint paired stress test, not a matched SOTA comparison.

### Failure-case figure

- Uses the exact original-protocol FSD checkpoint saved outputs associated with
  SHA-256 `5d7bac873b0b915fe6c0679b103fea9afe25f70de3958cab8da3d8779d156a37`.
- Cases are the three distinct scenes retained from the earlier qualitative
  audit, rerendered from the exact 22.91 output set rather than the old figure.
- Per-image CSV SHA-256:
  `af0256c8f7099811300eaaf20fcbfb8c7a5eb80975dc77c11d1b8a3fad566fba`.
- Evidence: `figure5_failure_cases_exact_manifest.json` records filenames,
  metrics, and hashes for every displayed low/prediction/ground-truth image.

## Awaiting clean evidence

- Frozen-protocol teaser/failure images: requires preserved checkpoint.
- Teacher-K and anchor-weight ablations: validation-only notebook is running.

## Newly retained

### Fixed-latent legacy-checkpoint step ablation

- Exact FSD checkpoint SHA-256: `5d7bac873b0b915fe6c0679b103fea9afe25f70de3958cab8da3d8779d156a37`.
- Evaluation uses DDIM, ARR $\alpha=0$, gate floor 0.5, the LOL-v2 Real official test split, and $N=100$.
- The same per-image latent sequence is reused for every step count (seed 99173).
- Evidence CSV: `results/step_ablation/table_vii_fixed_latent.csv`.
- Results: 5/10/20/50/100 steps produce 22.8790/22.7817/22.7144/22.6637/22.6430 dB PSNR and 0.8145/0.8130/0.8123/0.8118/0.8115 SSIM.
- The earlier saved-output rows are retained as `results/step_ablation/table_vii_legacy_saved_outputs.csv` and are not the fixed-latent evidence.

## Not restored

- Old cross-dataset table: generated by an older checkpoint while previously
  described as the headline checkpoint.
- Old ARR table: displayed values lacked a matching retained CSV.
- Old Teacher-K/anchor paragraph: mixed checkpoints, ARR settings, and test
  evaluations.
- Debug failure figure: raw filenames and debug title; may be regenerated from
  the preserved checkpoint.
- Architecture configuration table: redundant with the figure and prose.
- Expanded loss equations: compressed because the prose now matches code and
  preserves all implemented components.
